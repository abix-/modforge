# abioticfactor-mod open issues

Goal: play on Hard with scaled rewards, raise max skill level from 20 to 100.

Second goal: an AI player. A second player character that lives in the same world by the same rules as the human player, driven by the shared bot through player input. The game places it where it places any joining co-op player. It knows the map layout and the navigation mesh, but learns container contents and enemies only when it sees them. Near the human it walks with player input and obeys every rule. Far from the human it steps along a path the mesh returned, without physics, and never through a door it cannot open or a gap with no link. The first shipped state is one AI player that walks to a container and takes an item.

Status 2026-09-11: only the reads marked "Live" below have run against the game. Nothing has moved a character in this game, no second player has been created, no AI player code exists, and this mod has no function-call op yet.

| Priority | System | Todo | Done when |
|---:|---|---|---|
| 1 | `tests/research_ai_player.rs` | [x] Read the player character's movement values that decide jump and crouch links: jump speed, gravity scale, capsule radius and half height, crouched half height, max step height, walkable floor angle | Live 2026-09-11: JumpZVelocity 425, GravityScale 1.0, MaxStepHeight 45, WalkableFloorAngle 44.77, CrouchedHalfHeight 60, CapsuleRadius 32, CapsuleHalfHeight 96. Awaiting changelog entry before removal |
| 1 | `tests/research_ai_player.rs` | [x] Read the split-screen setting from the game maps settings object | Live 2026-09-11: bUseSplitscreen byte at +88 on the GameMapsSettings default object reads 1, so split-screen is on. Awaiting changelog entry before removal |
| 1 | `tests/research_ai_player.rs` | [ ] Add a PDB reader (the `pdb` crate as a dev-dependency, not yet added) and print the image-relative address of APlayerController::InputKey and UPlayerInput::InputKey plus the FInputKeyParams field layout | The test prints both addresses and the struct's fields with offsets and sizes from `AbioticFactor-Win64-Shipping.pdb`. Not started; the repo has no PDB reader |
| 1 | `tests/research_ai_player.rs` | [ ] Read the legacy input bindings from the InputSettings default object: action mapping names with their keys and axis mapping names with their keys | The test prints the move, look, jump, crouch, sprint, and interact mappings from the live game. Live 2026-09-11: 0 InputMappingContext and 0 InputAction assets loaded, so gameplay input is the legacy PlayerInput path, not Enhanced Input |
| 1 | `src/lib.rs` | [ ] Register the function-call op in this mod so tests can call UFunctions (MISERY registers one; this mod registers only the builtins) | `list_ops` on port 31260 lists the call op, and a test calls one harmless UFunction and gets a reply |
| 1 | `src/ai_player.rs` | [ ] Press and release W through InputKey on the human player's controller, at the address from the PDB, with the parameter block laid out from the PDB | The human player's character moves at least 100 units on press and stops on release, measured by a live test. Not started |
| 1 | `tests/research_ai_player.rs` | [ ] Print agent radius, height, and step height for every RecastNavMesh and name the one whose agent matches the player capsule (radius 32, height 192) | The test prints all 50 meshes and the match, or reports none. Live 2026-09-11: only the first mesh read, radius 80 and height 300, an enemy size |
| 1 | `tests/research_ai_player.rs` | [ ] Read the bounds volume's center and box extent through its brush component (replaces the tile count, which is not a reflected field) | The test prints center and extent of NavMeshBoundsVolume_0. Live 2026-09-11: 50 RecastNavMesh actors, 1 NavMeshBoundsVolume, 0 NavLinkProxy |
| 1 | `tests/research_ai_player.rs` | [ ] Ask the navigation system for a path from the human player to a chosen container through FindPathToLocationSynchronously and print its points | The test prints a complete path of at least two points on the player-sized mesh, or the failure reason. Needs the call op and the mesh match |
| 1 | `src/ai_player.rs` | [ ] Turn split-screen off by clearing bUseSplitscreen on the game maps settings default object | The byte reads 0 after the write and the game keeps running. Live 2026-09-11: byte at +88 reads 1; which bit inside it is bUseSplitscreen is not confirmed |
| 1 | `tests/research_ai_player.rs` | [ ] Create a second local player through GameplayStatics CreatePlayer in a loaded save and record what appears | The test prints the second character and its controller, the screen does not split, the HUD still draws, and the game answers 60 seconds later. Not attempted; needs the call op and the split-screen row |
| 1 | `tests/research_ai_player.rs` | [ ] Leave the second player idle two sectors from the human for five minutes and record what the game does to it | The test prints its health every 30 seconds, whether enemies reached it, and whether its sector stayed loaded. Decides whether far mode must hide the character |
| 1 | `src/ai_player.rs` | [ ] Move the AI player's character with W through InputKey on its own controller | A live test holds W for two seconds and the AI player's character moves at least 100 units while the human player's character does not move |
| 1 | `src/ai_player.rs` | [ ] Travel the AI player's character to a chosen container through the shared route and player input | A live test names one container, the route reaches it within interaction range, and every held key is released. Blocked on the workspace row "Return a complete Unreal navigation path" |
| 2 | `src/ai_player.rs` | [ ] Open the reached container and take one item with the AI player's own interact key through InputKey | The container's item count falls by one and the AI player's inventory count rises by one |
| 2 | `tests/research_ai_player.rs` | [ ] Read the AI player's needs: hunger, thirst, fatigue, and health, with their offsets on the character | The test prints each value from the live second character |
| 2 | `src/ai_player.rs` | [ ] Step the AI player along its path without physics when it is far from the human, at walking speed times elapsed time, never past a door it cannot open or a gap with no link | A live test parks the human, starts a far trip, and the AI player's position advances along the path points while its needs still change; a locked door stops it |
| 2 | `tests/research_ai_player.rs` | [ ] Read the player-sized mesh's polygons and their edges from the Detour tile data in memory, using the struct layouts from the PDB | The test prints the polygon count and, for one tile, each polygon's vertices and neighbor flags. Foundation for the workspace link-generation row |
| 3 | `src/ai_player.rs` | [ ] Keep the AI player's event log in a file per save: kills, deaths, containers opened, items taken, each with place and time | After a restart the test reads the file back and the last entry matches the last live event |
| 3 | `src/ai_player.rs` | [ ] Remember only what the AI player has seen: containers it opened and enemies it saw, in the same file as the event log | With a container never opened, the picker does not choose it for food; after it is opened once, it does |
| 1 | research | [x] Dump DT_SkillPerks: perk unlock levels and effects per skill | 101 perk rows confirmed, RequiredLevel field found via usmap |
| 1 | research | [x] Find max skill level and where it lives | No MaxLevel field in any struct. Level cap is a BP constant (20). SkillLevelMap at offset 2016, 48-byte entry stride, level at entry+32 |
| 1 | research | [x] Find XP curve: what XP is needed per level, how to extend it to level 100 | Vanilla table from wiki, flat 10,900 delta for 21-100. Documented in abiotic-factor.md |
| 1 | research | [ ] Find difficulty settings on GameMode or GameInstance | Difficulty fields listed with their types, offsets, and current values on a Hard save |
| 1 | mod | [ ] Raise skill cap to 100: hook XP award, bypass BP level cap check, write levels to SkillLevelMap | Skills can level past 20, confirmed by reading SkillLevelMap after XP gain |
| 2 | research | [ ] Find XP gain functions: what calls Server_TryAwardSkillXPOnTargetable, base rates | XP award call chain documented, base XP amounts per action type identified |
| 2 | research | [ ] Dump DT_DamageDefinitions: damage types and scaling values | Damage table rows documented, relationship to difficulty understood |
| 2 | research | [ ] Dump DT_BuffsDebuffs: buff/debuff values for potential reward scaling | Buff table rows documented, viable candidates for Hard reward buffs identified |
| 3 | mod | [ ] XP multiplier: scale XP gains on Hard to reward the challenge | XP gains on Hard are measurably higher than Normal |
| 3 | mod | [ ] Loot/reward scaling: better drops or crafting yields on Hard | Crafting or loot yields on Hard are measurably better |
| 3 | mod | [ ] Persist tweaks via tweaks.json (survives hot reload) | Tweaks survive Ctrl+R reload without manual re-apply |
